import { JsplumbDirective } from './jsplumb.directive';

describe('JsplumbDirective', () => {
  it('should create an instance', () => {
    const directive = new JsplumbDirective();
    expect(directive).toBeTruthy();
  });
});
